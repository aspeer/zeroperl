--- a/Makefile.PL
+++ b/Makefile.PL
@@ -13,8 +13,15 @@
     }
 }
 
-use inc::latest 'Capture::Tiny';
-use inc::latest 'Config::AutoConf';
+# Static WASI XS is qualified by the target build and runtime tests. Avoid
+# loading native probe dependencies through the target Perl library tree.
+BEGIN {
+    unless ($ENV{WASI_TARGET}) {
+        require inc::latest;
+        inc::latest->import('Capture::Tiny');
+        inc::latest->import('Config::AutoConf');
+    }
+}
 
 unless (-f "META.yml")
 {
@@ -35,10 +42,12 @@
 use ExtUtils::MakeMaker;
 
 use lib 'inc';
-use Config::AutoConf::ParamsUtil ();
-
-Config::AutoConf::ParamsUtil->_set_argv(@ARGV);    # XXX hack because we cannot construct for global use
-my $loadable_xs = Config::AutoConf::ParamsUtil->check_paramsutil_prerequisites();
+my $loadable_xs = 1;
+unless ($ENV{WASI_TARGET}) {
+    require Config::AutoConf::ParamsUtil;
+    Config::AutoConf::ParamsUtil->_set_argv(@ARGV);
+    $loadable_xs = Config::AutoConf::ParamsUtil->check_paramsutil_prerequisites();
+}
 
 # Should we build the XS version?
 my %RUN_DEPS = (
